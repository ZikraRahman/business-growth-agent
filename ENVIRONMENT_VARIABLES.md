# Environment Variables

NEXT_PUBLIC_API_URL=http://localhost:5678/webhook/business-agent

Never hardcode URLs.

Always read from environment variables.

The application should work by changing only the environment variable.