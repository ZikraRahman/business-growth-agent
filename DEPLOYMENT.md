# Deployment

Frontend

Deploy on Vercel.

Backend

Existing n8n deployment.

The frontend should never depend on localhost in production.

Switch automatically using environment variables.

Optimize production build.

Use lazy loading.

Optimize images.

Keep bundle size small.

Run production linting.

Run production build successfully before deployment.